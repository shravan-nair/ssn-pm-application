services:
  - type: web
    name: ssn-pm-application
    runtime: static
    buildCommand: echo "no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
